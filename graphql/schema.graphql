type ShoppingListItem @model @auth(rules: [{ allow: public }]) {
  id: ID!
  itemName: String!
  isComplete: Boolean
}

type Mutation {
  markShoppingDone(email: String!): [ShoppingListItem]
    @function(name: "markShoppingDoneFunction")
}
